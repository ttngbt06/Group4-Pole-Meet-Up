{{!-- <section class="painting">
  <img src="/images/{{painting.filename}}" alt="{{painting.description}}">
  <div class="painting-details">
    <h3>{{painting.title}}</h3>
    <p>By: {{painting.artist}}</p>
    <p>Exhibition open until: {{ format_date painting.exhibition_date}}</p>
  </div>
</section> --}}
{{#each options as |option|}}
<section>
  <p>{{option.name}}</p>
  <p>{{option.date_created}}</p>
  <h1>Polls</h1>
  
  <a href="/poll/{{option.poll.id}}">
    <header>{{option.poll.title}}</header>
    <p>Created By {{option.poll.user.name}}</p>
    <p>Expires On {{option.poll.expiration_date}}</p>
  </a>
  
</section>
{{/each}}